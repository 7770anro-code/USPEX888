# Smoke 2026-08-23 (локальный прогон агента, не прод)

Сквозной прогон: **идея → видео**. Постинг в соцсети **не выполнялся**.

## Что прошло

- Grok (`grok-4.5`) собрал 5 идей: 3 motivational + 2 absurd.
- Выбрана идея для аккаунта `motiv`: «Один таймер — весь день».
- Runway + ElevenLabs: 4 клипа 9:16, голос Уилл (пресет аккаунта, не клон).
- Оценка кредитов этого ролика: Runway ≈ **205**, ElevenLabs ≈ **297** символов.
- Файл: `720×1280` h264+aac, **40.1 с**, 5.2 МБ.
- Статус в БД: `wait_confirm` (публикация только после да/нет в Telegram).

Прогон оборвался на 4-м клипе; клип добран по сохранённому Runway `task_id` (resume), без новой генерации первых трёх.

## Что не готово

- Автопостинг: нет `NIGHT_ACC1_TIKTOK_ACCESS_TOKEN`, `NIGHT_ACC1_IG_ACCESS_TOKEN`, `NIGHT_ACC1_IG_USER_ID`.
- `NIGHT_AUTOPOST=0`, `NIGHT_REQUIRE_CONFIRM=1`.
- `NIGHT_OWNER_CHAT_ID` не задан — утренние кнопки в этот прогон не слались.
- На `/opt/videobot` **не деплоил**.

Секреты и значения токенов в этот файл не входят.
